;; this is the struct feature test file.
;; do not adhere to the semantics, as it may
;; change during development.

[let uint:hello]
[let int:c hello]

[define int:main [void]
	[let int:var 2]
	[console_create 1 2]
	
	[if [= var 2]
		[set var 3]]
	[elif [= var 5]
		[set c 10]]
	[else
		[set c 2]]]

[define console:console_create [int:x int:y]
	[set c [linux_console_create x y]]]

[struct console
	[uint:x]
	[uint:y]
	[platform_data:data]]

[struct uint]
[struct u32]
[struct platform_data]

[define void:linux_console_create [int:x int:y]
    [set hello 2]]